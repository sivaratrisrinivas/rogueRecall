# Corpus provenance

This directory preserves the frozen 50-case Benchmark Corpus provenance and
supporting review material. The runtime corpus-preparation and validation
commands were removed with the benchmark-only MVP, so these documents are
historical evidence rather than an operator workflow.

The `candidate-v1/` subdirectory contains the reviewed frozen candidate record,
source text, and Rights Evidence hashes that informed the packaged corpus.
